<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{path}} - Interplanetary File Browser</title>

  <link href="/assets/style.css" rel="stylesheet" />
</head>
<body>
  <div id="header" class="row">
    <div class="col-xs-2">
      <div id="logo" class="ipfs-logo"></div>
    </div>
  </div>
  <br>
  <div class="col-xs-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <strong>Index of {{path}}</strong>
      </div>
      <table class="table table-striped">
        <tbody>
          {{#if parent}}
          <tr>
            <td class="narrow">
              <div class="ipfs-icon ipfs-_blank">&nbsp;</div>
            </td>
            <td class="padding">
              <a href="{{parent}}">..</a>
            </td>
            <td></td>
          </tr>
          {{/if}}
          {{#each links as |link|}}
          <tr>
            <td>
              <div class="ipfs-icon ipfs-_blank">&nbsp;</div>
            </td>
            <td>
              <a href="{{link.url}}">{{link.name}}</a>
            </td>
            <td>
              {{link.humanReadableSize}}
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
